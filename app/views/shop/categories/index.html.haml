.container
    .columns
        .column.is-3.is-hidden-mobile
            =render 'shop/sidebar'
        .column.is-1.is-hidden-mobile
        .column.is-10-mobile.is-offset-1-mobile
            %table.table.is-hoverable
                %thead
                    %tr
                        %td{colspan:'3'}
                            %h2.text-large
                                %i.far.fa-sticky-note
                                作成したカテゴリ
                            %p.text-small カテゴリはメニューの見出しになります。
                %tbody
                    -if @categories.any?
                        - @categories.each do |category|
                            %tr
                                %td
                                    = category.name
                                %td
                                    .text-right
                                        = link_to root_path,class:'button is-info is-small is-rounded strong' do
                                            %i.fas.fa-edit
                                            編集
                                        = link_to shop_category_path(category),method: :delete,class:'button is-danger is-small is-rounded strong' do
                                            %i.fas.fa-trash
                                            削除
                    -else
                        %tr
                            %td{colspan:'3'}
                                カテゴリーがありません。
            %table.table
                %thead
                    %tr
                        %td{colspan:'2'}
                            %h2.text-large
                                %i.far.fa-paper-plane
                                カテゴリー新規追加
                %tbody
                    %tr
                        %td
                            = form_with model: Category.new,url:{:controller=>'shop/categories',:action=>'create'} do |f|
                                .field.has-addons
                                    .control
                                        = f.text_field :name,class:'input',placeholder:'新規カテゴリーを入力'
                                    .control
                                        = f.submit '追加',class:'button is-link'
                        %td
                            .text-right
                                = link_to shop_menus_path, class:'button is-primary' do
                                    %i.fas.fa-plus-circle
                                    メニューを作成