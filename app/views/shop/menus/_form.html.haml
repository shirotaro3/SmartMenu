= form_with model: menu,url: {:controller=>'shop/menus',:action=>'update'}, local: true do |f|
    .field
        %label メニュー名
        .control
            = f.text_field :menu_name,class:'input is-small'
    .field
        %label メニューの色
        .control
            = f.text_field :menu_color,class:'input is-small'
    .field
        %h3.text-large メニューアイテム
        %p.text-small メニューに載せる商品です。
    #menu-items
        = f.fields_for :menu_items do |menu_item|
            = render 'menu_item_fields', f: menu_item
        .links
            =link_to_add_association '項目を追加', f, :menu_items,class:'button is-success'
    .field
        .control
            = f.submit '登録',class:'button is-link'